# from Exercises.FindMax import utils  # imports the entire file
from Exercises.FindMax.utils import find_max  # import only the find_max method of the utils module/file

numbers = [1, 5, 3, 8, 7, -9]
print(find_max(numbers))
